<%= form_for [:admin, @user], :html => { :multipart => true, :class => "generic_form" } do |form| %>

	<div class="row">
		<%= form.label :role %>
		<%= form.select("role", ["member", "admin", "superadmin"]) %>
	</div>
	
	<div class="row">
		<%= submit_tag "Save" %>
	</div>
<% end %>