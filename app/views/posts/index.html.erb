<div id="posts_page">
	<h1>Blog</h1>

	<% if @posts.empty? %>
		<p>There are currently no posts.</p>
	<% else %>
	  <ol class="list">
		  <%= render :partial => 'post_mini', :collection => @posts %>
		</ol>
	<% end %>
	
	<%= will_paginate %>
</div>
